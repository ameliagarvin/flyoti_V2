
  # Make V2 Responsive

  This is a code bundle for Make V2 Responsive. The original project is available at https://www.figma.com/design/kqPa8pPjiI3MvpUDLCu3cI/Make-V2-Responsive.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  